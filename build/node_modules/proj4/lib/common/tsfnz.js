import { HALF_PI as f } from "../constants/values.js";
function p(a, o, r) {
  var t = a * r, n = 0.5 * a;
  return t = Math.pow((1 - t) / (1 + t), n), Math.tan(0.5 * (f - o)) / t;
}
export {
  p as default
};
