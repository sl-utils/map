import { HALF_PI as i } from "../constants/values.js";
function M(h, n) {
  for (var p = 0.5 * h, r, t, a = i - 2 * Math.atan(n), o = 0; o <= 15; o++)
    if (r = h * Math.sin(a), t = i - 2 * Math.atan(n * Math.pow((1 - r) / (1 + r), p)) - a, a += t, Math.abs(t) <= 1e-10)
      return a;
  return -9999;
}
export {
  M as default
};
