function i(a, r) {
  for (var v = 2 * Math.cos(2 * r), t = a.length - 1, h = a[t], n = 0, e; --t >= 0; )
    e = -n + v * h + a[t], n = h, h = e;
  return r + e * Math.sin(2 * r);
}
export {
  i as default
};
