import { __module as c } from "../../../_virtual/proj4leaflet2.js";
import { __require as p } from "../../leaflet/dist/leaflet-src.js";
import d from "../../../_virtual/index3.js";
var u;
function j() {
  return u ? c.exports : (u = 1, (function(h) {
    (function(o) {
      var s, t;
      s = p(), t = d, h.exports = o(s, t);
    })(function(o, s) {
      return s.__esModule && s.default && (s = s.default), o.Proj = {}, o.Proj._isProj4Obj = function(t) {
        return typeof t.inverse < "u" && typeof t.forward < "u";
      }, o.Proj.Projection = o.Class.extend({
        initialize: function(t, i, e) {
          var n = o.Proj._isProj4Obj(t);
          this._proj = n ? t : this._projFromCodeDef(t, i), this.bounds = n ? i : e;
        },
        project: function(t) {
          var i = this._proj.forward([t.lng, t.lat]);
          return new o.Point(i[0], i[1]);
        },
        unproject: function(t, i) {
          var e = this._proj.inverse([t.x, t.y]);
          return new o.LatLng(e[1], e[0], i);
        },
        _projFromCodeDef: function(t, i) {
          if (i)
            s.defs(t, i);
          else if (s.defs[t] === void 0) {
            var e = t.split(":");
            if (e.length > 3 && (t = e[e.length - 3] + ":" + e[e.length - 1]), s.defs[t] === void 0)
              throw "No projection definition for code " + t;
          }
          return s(t);
        }
      }), o.Proj.CRS = o.Class.extend({
        includes: o.CRS,
        options: {
          transformation: new o.Transformation(1, 0, -1, 0)
        },
        initialize: function(t, i, e) {
          var n, r, a, f;
          if (o.Proj._isProj4Obj(t) ? (r = t, n = r.srsCode, f = i || {}, this.projection = new o.Proj.Projection(r, f.bounds)) : (n = t, a = i, f = e || {}, this.projection = new o.Proj.Projection(n, a, f.bounds)), o.Util.setOptions(this, f), this.code = n, this.transformation = this.options.transformation, this.options.origin && (this.transformation = new o.Transformation(
            1,
            -this.options.origin[0],
            -1,
            this.options.origin[1]
          )), this.options.scales)
            this._scales = this.options.scales;
          else if (this.options.resolutions) {
            this._scales = [];
            for (var l = this.options.resolutions.length - 1; l >= 0; l--)
              this.options.resolutions[l] && (this._scales[l] = 1 / this.options.resolutions[l]);
          }
          this.infinite = !this.options.bounds;
        },
        scale: function(t) {
          var i = Math.floor(t), e, n, r, a;
          return t === i ? this._scales[t] : (e = this._scales[i], n = this._scales[i + 1], r = n - e, a = t - i, e + r * a);
        },
        zoom: function(t) {
          var i = this._closestElement(this._scales, t), e = this._scales.indexOf(i), n, r, a;
          return t === i ? e : i === void 0 ? -1 / 0 : (r = e + 1, n = this._scales[r], n === void 0 ? 1 / 0 : (a = n - i, (t - i) / a + e));
        },
        distance: o.CRS.Earth.distance,
        R: o.CRS.Earth.R,
        /* Get the closest lowest element in an array */
        _closestElement: function(t, i) {
          for (var e, n = t.length; n--; )
            t[n] <= i && (e === void 0 || e < t[n]) && (e = t[n]);
          return e;
        }
      }), o.Proj.GeoJSON = o.GeoJSON.extend({
        initialize: function(t, i) {
          this._callLevel = 0, o.GeoJSON.prototype.initialize.call(this, t, i);
        },
        addData: function(t) {
          var i;
          t && (t.crs && t.crs.type === "name" ? i = new o.Proj.CRS(t.crs.properties.name) : t.crs && t.crs.type && (i = new o.Proj.CRS(t.crs.type + ":" + t.crs.properties.code)), i !== void 0 && (this.options.coordsToLatLng = function(e) {
            var n = o.point(e[0], e[1]);
            return i.projection.unproject(n);
          })), this._callLevel++;
          try {
            o.GeoJSON.prototype.addData.call(this, t);
          } finally {
            this._callLevel--, this._callLevel === 0 && delete this.options.coordsToLatLng;
          }
        }
      }), o.Proj.geoJson = function(t, i) {
        return new o.Proj.GeoJSON(t, i);
      }, o.Proj.ImageOverlay = o.ImageOverlay.extend({
        initialize: function(t, i, e) {
          o.ImageOverlay.prototype.initialize.call(this, t, null, e), this._projectedBounds = i;
        },
        // Danger ahead: Overriding internal methods in Leaflet.
        // Decided to do this rather than making a copy of L.ImageOverlay
        // and doing very tiny modifications to it.
        // Future will tell if this was wise or not.
        _animateZoom: function(t) {
          var i = this._map.getZoomScale(t.zoom), e = o.point(this._projectedBounds.min.x, this._projectedBounds.max.y), n = this._projectedToNewLayerPoint(e, t.zoom, t.center);
          o.DomUtil.setTransform(this._image, n, i);
        },
        _reset: function() {
          var t = this._map.getZoom(), i = this._map.getPixelOrigin(), e = o.bounds(
            this._transform(this._projectedBounds.min, t)._subtract(i),
            this._transform(this._projectedBounds.max, t)._subtract(i)
          ), n = e.getSize();
          o.DomUtil.setPosition(this._image, e.min), this._image.style.width = n.x + "px", this._image.style.height = n.y + "px";
        },
        _projectedToNewLayerPoint: function(t, i, e) {
          var n = this._map.getSize()._divideBy(2), r = this._map.project(e, i)._subtract(n)._round(), a = r.add(this._map._getMapPanePos());
          return this._transform(t, i)._subtract(a);
        },
        _transform: function(t, i) {
          var e = this._map.options.crs, n = e.transformation, r = e.scale(i);
          return n.transform(t, r);
        }
      }), o.Proj.imageOverlay = function(t, i, e) {
        return new o.Proj.ImageOverlay(t, i, e);
      }, o.Proj;
    });
  })(c), c.exports);
}
export {
  j as __require
};
